# GetJokeAsXML

webMethods Integration Server Flow Service that calls the public Joke API and returns the result as XML.

- Namespace: `testFlows`
- Service: `GetJokeAsXML`
- Output: `xmlOutput` (String)
